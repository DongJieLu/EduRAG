# INTERVIEW.md — 高频追问与回答要点

围绕本项目设计决策的 10 个高频追问及回答要点（面试/答辩自述参考）。

## 1. 为什么做 FAQ + RAG 双通道？

**要点**：两类问题特征差异大。FAQ 类（"什么是 RAG？"）答案是确定、简短、可复用的，走 MySQL 精确检索即可毫秒级返回；复杂/开放问题需要跨片段归纳推理，必须走检索+生成的 RAG 链路。双通道把"快"和"深"解耦：高频短答不浪费 LLM 与检索成本，深度问题又不会被 FAQ 阈值误拦。分流依据是三层路由的输出 intent。

## 2. 路由阈值（0.90 / 0.75）是怎么定的？

**要点**：不是拍脑袋，是基于"误判代价"分层设计。语义相似度 ≥0.90 置信很高，直接判 FAQ，不再调用 LLM（省钱、省时）；0.75~0.90 是"灰色地带"，交给 L3 LLM 二次确认，只有 LLM 也判 faq 才进 FAQ；<0.75 说明大概率与 FAQ 库无关，直接进入 RAG/reject 判定。阈值在 EVALUATION.md 上有实测支撑（分流正确率 94%，reject 85%），是通过金标集反推校准过的。

## 3. HyDE（假设文档嵌入）什么时候有效，什么时候无效？

**要点**：当问题口语化、术语稀疏、与文档的"正式表述"有 gap 时有效——先用 LLM 生成一段假设性回答，再用这段"像文档的文本"去检索，能显著拉近 query 与 chunk 的语义距离。但它是"用一次生成换召回"，会多一次 LLM 调用、增加延迟；当问题本身术语已经很完整时（direct 即可）就是纯浪费，所以把选择权交给策略引擎按问题特征判断，而不是无条件开启。

## 4. 缓存一致性怎么取舍？

**要点**：容许多端短暂不一致，换取响应速度。答案缓存 TTL 1 小时、拒答 10 分钟；文档更新后按 direction 清理对应前缀 `qa:ans:{category}:*`（scan + del），属于"最终一致"。三级缓存的语义是：先查答案缓存，未命中查本 session 历史（同问复答），再未命中才落到 MySQL FAQ 精确兜底。热点 FAQ 单独用 zset 计数，用于统计与潜在的热点续期。

## 5. 混合检索的融合方案是什么？

**要点**：向量召回（BGE-M3 余弦）与关键词召回（ASCII 词元 + 中文 bigram 交集，从 MySQL chunk 文本匹配）两条路各自取 top50，用 **RRF（Reciprocal Rank Fusion，k=60）** 按排名倒数求和融合，不直接加权原始分数——因为向量分与词频分不在一个量纲，直接加权会被某一方主导，RRF 只关心"排名"更稳健。融合后再交给 bge-reranker 精排取 Top5。

## 6. 拒答是怎么设计的？

**要点**：双层把关。检索层：Top5 精排平均分 < 0.35 视为"无证据"，直接走拒答模板，不调 LLM（防止无中生有）。生成层：即便检索过了，若 LLM 输出含"未在资料中找到"等标记、或 JSON 解析失败/内容为空，也统一落到拒答模板。拒答同样写缓存（短 TTL），避免同类骚扰反复触发链路。

## 7. 评测集是怎么建的，怎么保证可复现？

**要点**：`scripts/gen_eval_set.py` 生成 100 条金标，faq:complex:reject = 3:5:2，每条带 `type`（期望 intent）、`expected_answer_keys`（关键信息点）、`retrieval_docs`（应命中文档）。`scripts/evaluate.py` 跑三组对比（纯向量基线 / 加重排 / 混合+策略），输出分流正确率、拒答正确率、Recall@5，并把复现命令 + 运行日期写进 EVALUATION.md。可复现 = 同一份金标 + 同一命令 + 记录运行时间。

## 8. 遇到过什么失败案例，怎么改进的？

**要点**：初版 L1 规则词把"是什么/如何/原理"这类宽泛词也当 FAQ 倾向词，导致大量 complex 问题（"RAG 的核心思想是什么？"）被误路由成 faq，分流正确率一度只有 77%。改进：把宽泛词从规则词里剔除，交给 L2 语义/L3 LLM 兜底，正确率回到 94%。这印证了"规则层只放高精度词，宽泛判断让模型做"的原则。另一个已知限制：当前语料只有 2 篇文档，Recall@5 三组都是 100%、无区分度，已在 EVALUATION.md 如实标注，需扩充语料后才能体现混合检索相对纯向量的提升。

## 9. 为什么不用 LangChain，而用原生 SDK 直连？

**要点**：本项目链路不复杂（路由→检索→重排→生成），LangChain 的抽象层带来的价值有限，却引入版本耦合与调试黑盒。直接用 openai（DeepSeek 兼容接口）、sentence-transformers、chromadb 三个库，代码路径清晰、可控、易单测。若后续需要更复杂的 Agent/工具编排再评估引入。

## 10. 可观测性/统计怎么做？

**要点**：每次问答结构化落库 `qa_log`（session_id / intent / strategy / route_detail / evidence_ids / latency_ms / cache_hit），统计接口从 qa_log 聚合意图分布、策略分布、平均延迟、日均序列，FAQ 命中数从 `faq.hit_count` 与 Redis 热点榜取。三层路由每层的分数与理由写入 route_detail，便于回溯"为什么这样分"。延迟分 FAQ / RAG 分开度量（FAQ 目标 <300ms、RAG 首字 <2s），当前端到端 p50/p95 尚未实测，标注为"待实测"。
