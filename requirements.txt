# ============================================================
# EduQA 依赖清单
# 说明：按里程碑分批安装；M1 仅需前四节（含 sentence-transformers）。
# ============================================================

# --- Web 框架 ---
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9        # 文件上传 multipart
httpx>=0.27                    # SSE 测试客户端

# --- 配置 ---
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0

# --- RAG 编排 ---
langchain>=0.3,<0.4
langchain-openai>=0.2
langchain-community>=0.3

# --- LLM / Embedding / Rerank ---
openai>=1.30                   # DeepSeek 走 OpenAI 兼容接口
sentence-transformers>=2.7     # BGE-M3 / bge-reranker-large

# --- 向量库 ---
chromadb>=0.5                  # 开发期向量库（M2）
pymilvus>=2.4                  # 生产向量库（M2 后期接入）

# --- 关系库 / 缓存 ---
pymysql>=1.1
sqlalchemy>=2.0
redis>=5.0

# --- 文档解析 ---
pypdf>=4.0
python-docx>=1.1
markdown>=3.5

# --- 前端 ---
gradio>=4.0

# --- 评估 ---
ragas>=0.1

# --- 测试 ---
pytest>=8.0
pytest-asyncio>=0.23
